# Disable service worker in development to prevent cache issues
REACT_APP_DISABLE_SERVICE_WORKER=true

# Disable fast refresh if causing issues (uncomment if needed)
# FAST_REFRESH=false

# Force webpack to not cache
GENERATE_SOURCEMAP=false